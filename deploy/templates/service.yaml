apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.env }}-site
  labels:
    app: site
    component: site
    env: {{ .Values.env }}
spec:
  selector:
    app: site
    component: site
    env: {{ .Values.env }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      name: http
