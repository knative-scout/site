apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.global.env }}-site
  labels:
    app: site
    component: site
    env: {{ .Values.global.env }}
spec:
  host: {{ template "site.host" . }}
  path: /
  to:
    kind: Service
    name: {{ .Values.global.env }}-site
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
status:
  ingress:
    - host: {{ template "site.host" . }}
