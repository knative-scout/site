import React from 'react'

export function AppHubSidebar(props: {categories: string[], tags: string[], onTagSelect : any, onCategorySelect : any}){
 /** Pull categories, tags, list as checkboxes, */
    return(
        <div className="ks-apphub-sidebar">
           
        </div>
    );
    
}